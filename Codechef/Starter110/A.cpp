#include <bits/stdc++.h>
using namespace std;
  int main (int argc, char *argv[]) {
    int w,h;
    cin >> w >> h;

    if(w >= 60 && h <= 130){
      cout << "YES\n";
    }else{
      cout << "NO\n";
    }
    return 0;
  }
